import config from "./config";
import redirect from './bootstrap';

// const { sex } = config

let app = new Vue({
        el: '#root',
        data: {
		},

        mounted() {
		},

        methods: {
        }
    })
;